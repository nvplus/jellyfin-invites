services:
  invites:
    build: .
    image: jellyfin-invites:latest
    restart: unless-stopped
    environment:
      DATABASE_PATH: /app/data/invites.db
      PORT: 8787
    ports:
      - "8787:8787"
    volumes:
      - ./data:/app/data
