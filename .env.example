# Optional. The app stores its config (Jellyfin URL, session secret) in the
# SQLite database; you'll set those up via the web UI on first launch.

# Where the sqlite file lives
DATABASE_PATH=./data/invites.db

# Server port
PORT=8787
